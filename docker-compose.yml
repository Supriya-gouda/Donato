version: '3'

services:

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    env_file:
      - ./frontend/.env

  backend:
    build: ./backend
    ports:
      - "5001:5000"
    env_file:
      - ./backend/.env
